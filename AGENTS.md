# cdc_omop_sjs

Phénotypage computationnel de la SjD sur données OMOP. Voir `CONTEXT.md` pour le vocabulaire et `docs/adr/` pour les décisions.

## Agent skills

### Issue tracker

Les issues vivent dans les GitHub Issues du dépôt `Nurtal/cdc_omop_sjs`, via la CLI `gh`. Voir `docs/agents/issue-tracker.md`.

### Triage labels

Libellés canoniques, sans renommage : `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. Voir `docs/agents/triage-labels.md`.

### Domain docs

Contexte unique : `CONTEXT.md` et `docs/adr/` à la racine. Voir `docs/agents/domain.md`.
